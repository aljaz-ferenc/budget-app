{
  "extends": "expo/tsconfig.base",
  // "extends": "@tsconfig/react-native/tsconfig.json",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "./src",
    "paths": {
      "*": ["*", "src/*"] // Allow imports from 'src' without relative paths
    }
  }
}
